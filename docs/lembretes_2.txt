UX/UI:

- Ajustes  menu lateral:
	- aonde temos o texto "Gestão de SKUs" altere apenas para SKUs 
		- no menu de cadastro o usuário saberá que é o cadastro de SKUs
		- no menu operacional saberá que é para fazer a gestão dos skus registrados
	- aonde temos o texto "controle de cores" altere apenas para cores 
		- no menu de cadastro o usuário saberá que é o cadastro de cores
		- no menu operacional saberá que é para fazer a gestão das cores registradas

- Ajuste por completo o acesso ao sistema 
	- Página de login deve validar usuário e quais seus acessos de acordo com as liberações no sistema 
	- Liberação de módulos para usuários comuns (operadores) 
	- liberação a nível de alteração de tabelas para usuários administradores 
	- todo esse controle deve ser feito via interface do sistema. 

- Ajuste também o formulário e a criação das tabelas para os formulários:
	- Clientes
	- Transportadoras
	- Representantes
	- Controle de cores

- Ajuste o botão de alteração e idioma para compor o menu lateral 
- Ajuste o menu lateral para quando os módulos expandidos icarem muito grande, habilitar scroll no menu lateral. 
- ajuste o ícone de notificações para comporta também o menu lateral



ROTA http://localhost:3000/admin/logs
- Crie as tabelas para exibir os ados dessa rota 
- padronize com o design geral do sistema. 


- Animação de flutuar ao posicionar o mouse em todas as telas igual a rota "/"
- Ao clicar no nome do módulo no dashboard inicial da rota "/" levar o usuário à página do módulo.

ROTA /cadastro
- Corrigir o percentual da qualidade geral de dados 
	- gerar uma tabela para registrar o calculo dessas variações 
	- exibir o percentual correta de evolução ou retrocesso da qualidade dos dados 

ROTA /cadastro/carregar-bases
- Corrigir upload de arquivos, apresenta erros no processamento de dados. Erro apresentad abaixo:

GET /api/estoque/arquivos-processados 200 in 721ms
 ○ Compiling /api/estoque/carregar-base ...
 ✓ Compiled /api/estoque/carregar-base in 6.3s (760 modules)
Erro no processamento: Error [PrismaClientKnownRequestError]:
Invalid `prisma.arquivoEstoqueProcessado.create()` invocation:


The provided value for the column is too long for the column's type. Column: erros
    at async eval (app\api\estoque\carregar-base\route.ts:198:33)
    at async POST (app\api\estoque\carregar-base\route.ts:196:23)
  196 |     const resultado = await prisma.$transaction(async (tx) => {
  197 |       // Criar registro do arquivo processado
> 198 |       const arquivoProcessado = await tx.arquivoEstoqueProcessado.create({
      |                                 ^
  199 |         data: {
  200 |           nomeArquivo,
  201 |           empresa, {
  code: 'P2000',
  clientVersion: '5.22.0',
  meta: [Object]
}
 POST /api/estoque/carregar-base 500 in 10538ms

ROTA http://localhost:3000/cadastro/empresas/1
- desenvolver página para exibir informações completas dos cadastros

ROTA http://localhost:3000/cadastro/clientes
	- Padronizar formulário ao estilo do restante da plataforma. 

ROTA /cadastro/fornecedores
	- Padronizar formulário ao estilo do restante da plataforma. 

ROTA /cadastro/transportadoras
	- Padronizar formulário ao estilo do restante da plataforma. 

ROTA http://localhost:3000/cadastro/representantes
	- Padronizar formulário ao estilo do restante da plataforma. 

ROTA /cadastro/representantes/novo
	- Padronizar formulário ao estilo do restante da plataforma. 
	- direcionar usuário para a rota /cadastro/representates após criar um novo registro 
	- botão cancelar está com erro ao clicar. 

ROTA /cadastro/fornecedores/novo
	- Padronizar formulário ao estilo do restante da plataforma. 
	- direcionar usuário para a rota /cadastro/fornecedores após criar um novo registro 
	- botão cancelar está com erro ao clicar. 

ROTA /cadastro/transportadoras/novo
	- Padronizar formulário ao estilo do restante da plataforma. 
	- direcionar usuário para a rota /cadastro/transportadoras após criar um novo registro 
	- botão cancelar está com erro ao clicar. 

ROTA /cadastro/clientes/novo
	- Padronizar formulário ao estilo do restante da plataforma. 
	- direcionar usuário para a rota /cadastro/transportadoras após criar um novo registro 
	- botão cancelar está com erro ao clicar. 

ROTA http://localhost:3000/cadastro/cores
	- Padronizar formulário ao estilo do restante da plataforma. 
	- Criar formulário padronizado de cadastro de cores 
	- as cores são cadastradas devem ter um número inteiro unico para compor a criação de SKUs, pois SKU é composto por "família", "cor" e "tamanho" 

ROTA http://localhost:3000/cadastro
	- Criar novos módulos de cadastro: 'Familia" e "tamanho"
	- ambos devem ser criados com códigos números para compor o SKU 
	- Criar um módulo chamado "UNEGs" (Unidades de negócio) para cadastro de categorização dos SKUs
	- UNEG é um complemento de um SKU durante sua criação. 
	- Crie um módulo para criação de depósitos
		- Cada empresa cadastrada pode receber cadastros de mvarios depósitos 
		- cada depósito também pode ter materiais de mais de uma empresa 


ROTA http://localhost:3000/cadastro/skus/novo
	- Melhorias para o formulário:
		- código do SKU: deve ser um campo que não pode ser alterado e vá se formando conforme o usuário informe os dados 
		- SKU é composto por concatecado e "Familia" "Tamanho" e "Cor"
		- esses 3 campos devem ser escolhidos da lista dos cadastros já existentes
		- os campos devem aceitar seleção e busca com sugestão de resultados próximos ao usuário, os 3 campos devem se comportar assim 
		- Categoria é UNEG (Unidade de negócio) 
		- crie uma tabela para cadastrar UNEGs
		- Mantenha Nome do produto, descrição do produto, estoque mínimo e estoque máximo
		- REMOVA:
			-Preço de venda
			- Custo Médio

ROTA http://localhost:3000/executivo
	- Corrigir o card "Total de Produtos", deve fazer contagem de SKUs individuais cadastrados 
	- Estoque total deverá apresentar o montante em Metros do estoque geral no sistema 
	- Corrigir alertas e ações necessárias para refletir necessidades e alertas reais do sistema de acordo com os dados do banco de dados 
	- Módulos executivos:
		- Todos eles devem apresentar os números reais calculando com base nos dados dos bancos de dados
	- Criar submódulo de Metas 
		- desenvolva o dashboard e a sistemática para esse módulo
		- deverá ter cadastros de metas de compra e metas de vendas, tanto em valor, quanto em metragem de produtos, 
		- iremos definir as metas já travadas sistemicamente para cada representante cadastrado com SKUs exatos e assim por diante 
		- todos os dados precisam passar a se relacionar para construir os insights necessários

ROTA http://localhost:3000/executivo/cores
- os cards dessa rota devem refletir informações de acordo com o cadastro de cores 
- Remova o botão de criar nova cor, para centralizar a criação de cores no módulo de cadastros


ROTA http://localhost:3000/executivo/cores/COR-001
- Criar página com detalhes completos e dados relacionados de cada cor individualmente

ROTA http://localhost:3000/executivo/skus
- todos os dados dessa rota devem seguir o padrão de design das demais paginas do sistema
- os cards devem representar cálculos e informações reais baseadas no banco de dados 


ROTA http://localhost:3000/executivo/precos
- todos os dados dessa rota devem seguir o padrão de design das demais paginas do sistema
- os cards devem representar cálculos e informações reais baseadas no banco de dados 

ROTA http://localhost:3000/executivo/estoque
- todos os dados dessa rota devem seguir o padrão de design das demais paginas do sistema
- os cards devem representar cálculos e informações reais baseadas no banco de dados 
- mantenha todas as informações já do MVP da forma que está e apenas alterar as lógicas do sistema e relacionamentos das tabelas para que os dados reflitam nas informações definidas no MVP
- Os dados para posição de estoque devem ser gerados com as tabelas que será populada pelo usuário no carregamento de bases do módulo de cadastro

ROTA http://localhost:3000/planejamento
- todos os dados dessa rota devem seguir o padrão de design das demais paginas do sistema
- os cards devem representar cálculos e informações reais baseadas no banco de dados 

ROTA http://localhost:3000/admin
- todos os dados dessa rota devem seguir o padrão de design das demais paginas do sistema
- os cards devem representar cálculos e informações reais baseadas no banco de dados 


ATUALIZAR DOCUMENTAÇÃO:

- módulo de cadastro de empresas rota /cadastro/empresas está concluído e finalizado.
____________________

INTRUÇÕES DO GERENTE:

Módulo executivo 
	- Meta de vendas
	- Margem alvo 
	- Margem real 

- Análise de estoque 
	- com mudança de parâmetros 
		- Meta de venda 
		- Estoque objetivo (Quero ter x quantidade de SKU em estoque por cor)
		- Aplicar a lógica do BI Analise de estoque dentro da página para alterar parâmetros da Meta e quantidade de estoque para retornar a sugestão de compra 
	- Botão para distribuir cores 
		- Distribuir as cores baseando em parâmetros como (quantidade de estoque ou meta) 
		- essa etapa será a validação final para direcionar para criação da OC
		- Botão de inclusão do produto na OC 
	- OCs Pendentes 





- Cadastra
- analisa o estoque 
- gera recomendação no produto
- aprovando  recomendação do produto
- gera recomendação de distribuição de cores
- aprova distribuição de cores 
- Aprova inclusão na OC pendente
-  visualiza a OC pendente
- aprova a OC pendente 
- libera ao financeiro 
- Aprova pelo financeiro 
- Após essas etapas é notificado os responsáveis por compras
- a definição de fornecedor para cada produto da OC é definida pelo time de compras
- A cada compra de produto da OC é confirmado no sistema atualizando o percentual atendido da OC (ver exemplo de email de confirmação de compra)
- Com a confirmação da compra é emitido o PO number 
- 


_